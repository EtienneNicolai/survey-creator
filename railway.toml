[build]
builder = "nixpacks"
buildCommand = "cd frontend && npm install && npm run build"

[deploy]
startCommand = "PYTHONPATH=/app python -m alembic upgrade head && PYTHONPATH=/app python -m uvicorn backend.src.api.main:app --host 0.0.0.0 --port $PORT"
